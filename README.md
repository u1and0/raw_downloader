## Usage:

```shell-session
$ python raw_downloader.py
```

スキップする話数、出力ディレクトリを調整する。

## Install:

```shell-session
$ yay -Sy chromedriver
$ conda install -yc conda-forge selenium
$ yay -S google-chrome
```
